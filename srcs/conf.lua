-- 160x144 = gb size
-- but it's very smoll, so i did a x4
-- for the tiles: gb orig 8x8 so now 32x32
function love.conf(t)
	-- t.window.width = 640 -- gb orig 160
	-- t.window.height = 480 -- gb orig 144
	t.window.title = "Escape the Mansion"
end